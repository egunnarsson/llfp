module foo

i32 i = 1;

i32 foo(i32 x) = 2;

i32 bar(i32 y, i32 z) =
    let i32 baz = 1;
    in x() + x + y + baz;

i32 meh(bool b, i32 x) = if b then 1 + x else 2;

i32 x = meh(true, 1);

i32 not_test(bool b, i32 i1, i32 i2) = if !b then ~i1 else i1 + i2;
